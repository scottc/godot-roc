# engine class ResourceImporterDynamicFont → ResourceImporterDynamicFont
# api_type: editor
# instantiable, refcounted
# inherits: ResourceImporter
ResourceImporterDynamicFont := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}