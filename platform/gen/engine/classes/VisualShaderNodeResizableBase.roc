# engine class VisualShaderNodeResizableBase → VisualShaderNodeResizableBase
# api_type: core
# not instantiable, refcounted
# inherits: VisualShaderNode
VisualShaderNodeResizableBase := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---
    # property size : Vector2
    #   getter: get_size
    #   setter: set_size

    # --- methods ---
    # set_size!  is_const=False is_static=False is_vararg=False
    #set_size! : Vector2 -> {}
    #set_size! = Host.set_size_743155724!
    # get_size!  is_const=True is_static=False is_vararg=False
    #get_size! : () -> Vector2
    #get_size! = Host.get_size_3341600327!

    # --- signals ---

}