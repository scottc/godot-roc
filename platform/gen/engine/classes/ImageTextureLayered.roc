# engine class ImageTextureLayered → ImageTextureLayered
# api_type: core
# not instantiable, refcounted
# inherits: TextureLayered
ImageTextureLayered := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # create_from_images!  is_const=False is_static=False is_vararg=False
    #create_from_images! : typedarray::Image -> enum::Error
    #create_from_images! = Host.create_from_images_2785773503!
    # update_layer!  is_const=False is_static=False is_vararg=False
    #update_layer! : Image, I32 -> {}
    #update_layer! = Host.update_layer_3331733361!

    # --- signals ---

}