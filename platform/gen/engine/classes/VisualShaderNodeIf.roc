# engine class VisualShaderNodeIf → VisualShaderNodeIf
# api_type: core
# instantiable, refcounted
# inherits: VisualShaderNode
VisualShaderNodeIf := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}