# engine class AudioEffect → AudioEffect
# api_type: core
# instantiable, refcounted
# inherits: Resource
AudioEffect := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # _instantiate!  is_const=False is_static=False is_vararg=False
    #_instantiate! : () -> AudioEffectInstance
    #_instantiate! = Host._instantiate_1659796816!

    # --- signals ---

}