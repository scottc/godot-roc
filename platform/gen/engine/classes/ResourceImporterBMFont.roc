# engine class ResourceImporterBMFont → ResourceImporterBMFont
# api_type: editor
# instantiable, refcounted
# inherits: ResourceImporter
ResourceImporterBMFont := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}