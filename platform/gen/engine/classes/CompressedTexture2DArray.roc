# engine class CompressedTexture2DArray → CompressedTexture2DArray
# api_type: core
# instantiable, refcounted
# inherits: CompressedTextureLayered
CompressedTexture2DArray := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}