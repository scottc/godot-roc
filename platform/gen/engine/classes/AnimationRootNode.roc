# engine class AnimationRootNode → AnimationRootNode
# api_type: core
# instantiable, refcounted
# inherits: AnimationNode
AnimationRootNode := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}