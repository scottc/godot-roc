# engine class VisualShaderNodeCurveTexture → VisualShaderNodeCurveTexture
# api_type: core
# instantiable, refcounted
# inherits: VisualShaderNodeResizableBase
VisualShaderNodeCurveTexture := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---
    # property texture : CurveTexture
    #   getter: get_texture
    #   setter: set_texture

    # --- methods ---
    # set_texture!  is_const=False is_static=False is_vararg=False
    #set_texture! : CurveTexture -> {}
    #set_texture! = Host.set_texture_181872837!
    # get_texture!  is_const=True is_static=False is_vararg=False
    #get_texture! : () -> CurveTexture
    #get_texture! = Host.get_texture_2800800579!

    # --- signals ---

}