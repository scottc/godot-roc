# class ImageTexture3D → ImageTexture3D
# inherits: Texture3D
ImageTexture3D := {
    ptr : U64,
}.{


    # create! : enum::Image.Format, I32, I32, I32, Bool, typedarray::Image -> enum::Error
    # create! = Host.create_1130379827!
    # update! : typedarray::Image -> {}
    # update! = Host.update_381264803!

}