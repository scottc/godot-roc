# engine class FramebufferCacheRD → FramebufferCacheRD
# api_type: core
# instantiable, not refcounted
# inherits: Object
FramebufferCacheRD := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # get_cache_multipass!  is_const=False is_static=True is_vararg=False
    #get_cache_multipass! : typedarray::RID, typedarray::RDFramebufferPass, I32 -> RID
    #get_cache_multipass! = Host.get_cache_multipass_3437881813!

    # --- signals ---

}