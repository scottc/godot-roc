# engine class PackedDataContainer → PackedDataContainer
# api_type: core
# instantiable, refcounted
# inherits: Resource
PackedDataContainer := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # pack!  is_const=False is_static=False is_vararg=False
    #pack! : Variant -> enum::Error
    #pack! = Host.pack_966674026!
    # size!  is_const=True is_static=False is_vararg=False
    #size! : () -> I32
    #size! = Host.size_3905245786!

    # --- signals ---

}