# engine class PlaceholderCubemapArray → PlaceholderCubemapArray
# api_type: core
# instantiable, refcounted
# inherits: PlaceholderTextureLayered
PlaceholderCubemapArray := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}