# engine class GDScript → GDScript
# api_type: core
# instantiable, refcounted
# inherits: Script
GDScript := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # new!  is_const=False is_static=False is_vararg=True
    #new! : () -> Variant
    #new! = Host.new_1545262638!

    # --- signals ---

}