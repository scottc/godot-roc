# engine class OpenXRVisibilityMask → OpenXRVisibilityMask
# api_type: core
# instantiable, not refcounted
# inherits: VisualInstance3D
OpenXRVisibilityMask := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}