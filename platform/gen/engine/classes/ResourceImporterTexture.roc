# engine class ResourceImporterTexture → ResourceImporterTexture
# api_type: editor
# instantiable, refcounted
# inherits: ResourceImporter
ResourceImporterTexture := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}