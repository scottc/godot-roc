# engine class OpenXRInteractionProfileEditorBase → OpenXRInteractionProfileEditorBase
# api_type: editor
# not instantiable, not refcounted
# inherits: HBoxContainer
OpenXRInteractionProfileEditorBase := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # setup!  is_const=False is_static=False is_vararg=False
    #setup! : OpenXRActionMap, OpenXRInteractionProfile -> {}
    #setup! = Host.setup_421962938!

    # --- signals ---

}