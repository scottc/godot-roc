# engine class VisualShaderNodeCurveXYZTexture → VisualShaderNodeCurveXYZTexture
# api_type: core
# instantiable, refcounted
# inherits: VisualShaderNodeResizableBase
VisualShaderNodeCurveXYZTexture := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---
    # property texture : CurveXYZTexture
    #   getter: get_texture
    #   setter: set_texture

    # --- methods ---
    # set_texture!  is_const=False is_static=False is_vararg=False
    #set_texture! : CurveXYZTexture -> {}
    #set_texture! = Host.set_texture_8031783!
    # get_texture!  is_const=True is_static=False is_vararg=False
    #get_texture! : () -> CurveXYZTexture
    #get_texture! = Host.get_texture_1950275015!

    # --- signals ---

}