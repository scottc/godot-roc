# engine class PackedDataContainerRef → PackedDataContainerRef
# api_type: core
# not instantiable, refcounted
# inherits: RefCounted
PackedDataContainerRef := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # size!  is_const=True is_static=False is_vararg=False
    #size! : () -> I32
    #size! = Host.size_3905245786!

    # --- signals ---

}