# engine class CompressedCubemap → CompressedCubemap
# api_type: core
# instantiable, refcounted
# inherits: CompressedTextureLayered
CompressedCubemap := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}