# engine class VisualShaderNodeParticleMultiplyByAxisAngle → VisualShaderNodeParticleMultiplyByAxisAngle
# api_type: core
# instantiable, refcounted
# inherits: VisualShaderNode
VisualShaderNodeParticleMultiplyByAxisAngle := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---
    # property degrees_mode : Bool
    #   getter: is_degrees_mode
    #   setter: set_degrees_mode

    # --- methods ---
    # set_degrees_mode!  is_const=False is_static=False is_vararg=False
    #set_degrees_mode! : Bool -> {}
    #set_degrees_mode! = Host.set_degrees_mode_2586408642!
    # is_degrees_mode!  is_const=True is_static=False is_vararg=False
    #is_degrees_mode! : () -> Bool
    #is_degrees_mode! = Host.is_degrees_mode_36873697!

    # --- signals ---

}