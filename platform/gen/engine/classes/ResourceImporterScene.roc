# engine class ResourceImporterScene → ResourceImporterScene
# api_type: editor
# instantiable, refcounted
# inherits: ResourceImporter
ResourceImporterScene := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}