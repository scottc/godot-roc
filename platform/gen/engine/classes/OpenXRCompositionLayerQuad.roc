# engine class OpenXRCompositionLayerQuad → OpenXRCompositionLayerQuad
# api_type: core
# instantiable, not refcounted
# inherits: OpenXRCompositionLayer
OpenXRCompositionLayerQuad := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---
    # property quad_size : Vector2
    #   getter: get_quad_size
    #   setter: set_quad_size

    # --- methods ---
    # set_quad_size!  is_const=False is_static=False is_vararg=False
    #set_quad_size! : Vector2 -> {}
    #set_quad_size! = Host.set_quad_size_743155724!
    # get_quad_size!  is_const=True is_static=False is_vararg=False
    #get_quad_size! : () -> Vector2
    #get_quad_size! = Host.get_quad_size_3341600327!

    # --- signals ---

}