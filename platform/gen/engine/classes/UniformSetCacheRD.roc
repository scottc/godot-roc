# engine class UniformSetCacheRD → UniformSetCacheRD
# api_type: core
# instantiable, not refcounted
# inherits: Object
UniformSetCacheRD := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # get_cache!  is_const=False is_static=True is_vararg=False
    #get_cache! : RID, I32, typedarray::RDUniform -> RID
    #get_cache! = Host.get_cache_658571723!

    # --- signals ---

}