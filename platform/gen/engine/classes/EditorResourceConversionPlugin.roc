# engine class EditorResourceConversionPlugin → EditorResourceConversionPlugin
# api_type: editor
# instantiable, refcounted
# inherits: RefCounted
EditorResourceConversionPlugin := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # _converts_to!  is_const=True is_static=False is_vararg=False
    #_converts_to! : () -> String
    #_converts_to! = Host._converts_to_201670096!
    # _handles!  is_const=True is_static=False is_vararg=False
    #_handles! : Resource -> Bool
    #_handles! = Host._handles_3190994482!
    # _convert!  is_const=True is_static=False is_vararg=False
    #_convert! : Resource -> Resource
    #_convert! = Host._convert_325183270!

    # --- signals ---

}