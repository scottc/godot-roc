# engine class InputEventShortcut → InputEventShortcut
# api_type: core
# instantiable, refcounted
# inherits: InputEvent
InputEventShortcut := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---
    # property shortcut : Shortcut
    #   getter: get_shortcut
    #   setter: set_shortcut

    # --- methods ---
    # set_shortcut!  is_const=False is_static=False is_vararg=False
    #set_shortcut! : Shortcut -> {}
    #set_shortcut! = Host.set_shortcut_857163497!
    # get_shortcut!  is_const=False is_static=False is_vararg=False
    #get_shortcut! : () -> Shortcut
    #get_shortcut! = Host.get_shortcut_3766804753!

    # --- signals ---

}