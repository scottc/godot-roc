# engine class PlaceholderCubemap → PlaceholderCubemap
# api_type: core
# instantiable, refcounted
# inherits: PlaceholderTextureLayered
PlaceholderCubemap := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}