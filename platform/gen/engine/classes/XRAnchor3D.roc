# engine class XRAnchor3D → XRAnchor3D
# api_type: core
# instantiable, not refcounted
# inherits: XRNode3D
XRAnchor3D := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # get_size!  is_const=True is_static=False is_vararg=False
    #get_size! : () -> Vector3
    #get_size! = Host.get_size_3360562783!
    # get_plane!  is_const=True is_static=False is_vararg=False
    #get_plane! : () -> Plane
    #get_plane! = Host.get_plane_2753500971!

    # --- signals ---

}