# engine class RenderSceneBuffers → RenderSceneBuffers
# api_type: core
# not instantiable, refcounted
# inherits: RefCounted
RenderSceneBuffers := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # configure!  is_const=False is_static=False is_vararg=False
    #configure! : RenderSceneBuffersConfiguration -> {}
    #configure! = Host.configure_3072623270!

    # --- signals ---

}