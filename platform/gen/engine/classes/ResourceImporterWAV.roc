# engine class ResourceImporterWAV → ResourceImporterWAV
# api_type: editor
# instantiable, refcounted
# inherits: ResourceImporter
ResourceImporterWAV := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}