# engine class SkeletonModification2DStackHolder → SkeletonModification2DStackHolder
# api_type: core
# instantiable, refcounted
# inherits: SkeletonModification2D
SkeletonModification2DStackHolder := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---
    # set_held_modification_stack!  is_const=False is_static=False is_vararg=False
    #set_held_modification_stack! : SkeletonModificationStack2D -> {}
    #set_held_modification_stack! = Host.set_held_modification_stack_3907307132!
    # get_held_modification_stack!  is_const=True is_static=False is_vararg=False
    #get_held_modification_stack! : () -> SkeletonModificationStack2D
    #get_held_modification_stack! = Host.get_held_modification_stack_2107508396!

    # --- signals ---

}