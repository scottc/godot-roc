# engine class CompressedCubemapArray → CompressedCubemapArray
# api_type: core
# instantiable, refcounted
# inherits: CompressedTextureLayered
CompressedCubemapArray := {
    # opaque host handle — concrete layout lives on the Zig side
    ptr : U64,
}.{
    # --- enums ---


    # --- properties ---


    # --- methods ---


    # --- signals ---

}